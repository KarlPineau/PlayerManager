{# src/DnDRules/ClassDnDBundle/Resources/views/ClassDnD/register_content.html.twig #}

<form class="form-horizontal" method="post" action="{{ path('dndrules_classdnd_classdnd_edit_st', {'slug': classDnD.slug}) }}" {{ form_enctype(form) }} role="form">
    {% if not form.vars.valid %}
    <div class="alert alert-error">
        <strong>Votre formulaire contient les erreurs suivantes :</strong>
        {{ form_errors(form) }}
        {{ form_errors(form.levels) }}
    </div>
    <br />
    {% endif %}

    <fieldset>
        <legend>Jets de Sauvegarde :</legend>

        {{ form_widget(form.levels) }}
    </fieldset>
        <br />
    {{ form_rest(form) }}
    <div class="text-center"><input type="submit" class="btn btn-default" /></div>
</form>